##############################################################################
# Copyright (c) 2019-2021, Lawrence Livermore National Security, LLC and
# RADIUSS project contributors. See the COPYRIGHT file for details.
#
# SPDX-License-Identifier: (MIT)
##############################################################################

.on-corona:
  tags: [shell, corona]

.corona-variables:
  variables:
    SPACK_DISABLE_LOCAL_CONFIG: ""
    MY_SPACK_DEBUG: ""
#[create-unique-path--]
    # Spack clone for corona generated pipeline
    MY_SPACK_PARENT_DIR: ${CUSTOM_CI_BUILDS_DIR}/llnl-stack-${CI_PIPELINE_ID}-corona
    # Spack cache for spack at pipeline generation time, then overridden by each CI job
    SPACK_USER_CACHE_PATH: ${CUSTOM_CI_BUILDS_DIR}/llnl-stack-${CI_PIPELINE_ID}-corona/spack-user-cache
    # Spack cache for each CI job in the generated pipeline (avoids collision)
    MY_SPACK_USER_CACHE: /dev/shm/llnl-stack-${CI_PIPELINE_ID}-${CI_JOB_ID}/spack-cache
#[--create-unique-path]

.on-ruby:
  tags: [shell, ruby]

.ruby-variables:
  variables:
    SPACK_DISABLE_LOCAL_CONFIG: ""
    MY_SPACK_DEBUG: ""
#[create-unique-path--]
    # Spack clone for ruby generated pipeline
    MY_SPACK_PARENT_DIR: ${CUSTOM_CI_BUILDS_DIR}/llnl-stack-${CI_PIPELINE_ID}-ruby
    # Spack cache for spack at pipeline generation time, then overridden by each CI job
    SPACK_USER_CACHE_PATH: ${CUSTOM_CI_BUILDS_DIR}/llnl-stack-${CI_PIPELINE_ID}-ruby/spack-user-cache
    # Spack cache for each CI job in the generated pipeline (avoids collision)
    MY_SPACK_USER_CACHE: /dev/shm/llnl-stack-${CI_PIPELINE_ID}-${CI_JOB_ID}/spack-cache
#[--create-unique-path]

.on-lassen:
  tags: [shell, lassen]

.lassen-variables:
  variables:
    MY_SPACK_DEBUG: ""
    SPACK_DISABLE_LOCAL_CONFIG: ""
    # Spack clone for ruby generated pipeline
    MY_SPACK_PARENT_DIR: ${CUSTOM_CI_BUILDS_DIR}/llnl-stack-${CI_PIPELINE_ID}-lassen
    # Spack cache for spack at pipeline generation time, then overridden by each CI job
    SPACK_USER_CACHE_PATH: ${CUSTOM_CI_BUILDS_DIR}/llnl-stack-${CI_PIPELINE_ID}-lassen/spack-user-cache
    # Spack cache for each CI job in the generated pipeline (avoids collision)
    MY_SPACK_USER_CACHE: /dev/shm/llnl-stack-${CI_PIPELINE_ID}-${CI_JOB_ID}/spack-cache

# Generate CI pipelines for the package in $MY_ENV_NAME
.generate-pipeline:
  script:
    - . ${MY_SPACK_PARENT_DIR}/spack/share/spack/setup-env.sh
    - spack ${MY_SPACK_DEBUG} env activate --without-view spack-environments/${MY_ENV_NAME}
    - spack ${MY_SPACK_DEBUG} config blame mirrors
    - spack ${MY_SPACK_DEBUG} mirror add --oci-username ${CI_REGISTRY_USER} --oci-password ${CI_REGISTRY_PASSWORD} buildcache-destination oci://${CI_REGISTRY_IMAGE}
    - spack ${MY_SPACK_DEBUG} config blame mirrors
#    - spack ${MY_SPACK_DEBUG} concretize || spack ${MY_SPACK_DEBUG} concretize
    - spack ${MY_SPACK_DEBUG} --color=always
        --config-scope "${CI_PROJECT_DIR}/.gitlab/spack"
        ci generate
        --check-index-only
        --artifacts-root "${CI_PROJECT_DIR}/jobs_scratch_dir"
        --output-file "${CI_PROJECT_DIR}/jobs_scratch_dir/pipeline.yml"
    - cp -r spack-environments/${MY_ENV_NAME}/${LCSCHEDCLUSTER} ${CI_PROJECT_DIR}/jobs_scratch_dir/concrete_environment/
  artifacts:
    paths:
      - "${CI_PROJECT_DIR}/jobs_scratch_dir"
